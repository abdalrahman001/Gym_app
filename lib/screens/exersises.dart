import 'package:flutter/material.dart';

class Exersises extends StatelessWidget {
  const Exersises({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('holooo'),
    );
  }
}
